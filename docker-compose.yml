services:
  nai-gate:
    build: .
    container_name: nai-gate
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - DATA_DIR=/app/data
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
