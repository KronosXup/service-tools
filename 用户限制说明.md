# 用户限制说明

用户身份以一把 `nai-...` 虚拟 Key 为单位。多人共享同一把 Key 时，共享全部额度与限流。

## 基础限制

| 项目 | 当前规则 |
| --- | --- |
| Key 有效期 | 由管理员创建时设置 |
| 长期未使用回收 | 超过 3 天没有通过鉴权的请求（从未使用按创建时间算）将自动永久删除 Key |
| 请求频率 | 每把 Key 最多 10 次/分钟 |
| 同 Key 并发 | 1 个请求；后续请求排队 |
| 图片/标签任务冷却 | 每把普通用户 Key 至少间隔 15 秒；冷却等待会自动排队 |
| 图片模型范围 | 每把 Key 单独设为“仅 V4.5 及更低”或“所有当前支持模型（含 V5）” |
| 图片统计 | 成功出图按实际张数统计；V4.5 及以下免费图默认每 Key 每日 100 张，付费图不占此额度 |
| 图片失败或被拒绝 | 未收到完整图片不记费；流式部分成功按已完成张数结算 |
| 文本 | 每日最多 150,000 输出 tokens；单次最多 300 输出 tokens，输入最多 24,000 字符 |
| img2img / 局部重绘 | 默认关闭，需全局及 Key 同时允许；计费条件与文生图一致 |

## V4.5 及更早模型

V4.5 等非 V5 模型的免费生图每 Key 默认每天最多 100 张；达到上限后要等次日恢复。付费规格只受 Anlas 额度、RPM、排队、用户 Key 冷却、上游 Token 间隔和上游冷却保护。

只有被授予“仅 V4.5 及更低”或“所有当前支持模型”权限的 Key 可使用本节模型；前者不能请求 V5。是否可使用旧模型的付费规格，仍单独由该 Key 的 Anlas 权限决定。

Opus 在以下条件下减免首张基础生成费，文生图、图生图和局部重绘均适用：

- 最多 28 steps
- 像素面积不超过 `1024×1024`，不要求标准长宽比
- 不使用 ControlNet 等额外付费条件；SMEA 不影响首张减免

同一请求的第二张起支付基础费；精确参考和超过四张的 V4/V4.5 Vibe 另收附加费。
符合首张减免条件的多图请求也减免首张参考附加费，单张请求仍收一份附加费。
没有附加费的首张免费图占一个本地免费图日额；付费输出不占此额度。

其他 V4.5 规格会产生 Anlas 消耗：

- Key 未开通 Anlas：请求自动调整到最近的免费 Normal 规格；
- Key 已开通 Anlas：按 Anlas 估算扣费，受每日与每月 Anlas 额度限制。

## V5 模型

V5 使用独立额度，不走非 V5 的无限免费规则；仅“所有当前支持模型（含 V5）”权限的 Key 可用。该权限不自动开放 Anlas，V5 的额度外/付费规格仍需该 Key 开启 Anlas 权限。

官方额度可用，且最多 28 steps、面积不超过 `1024×1024` 时，首张扣 1 个 V5 额度，
免基础 Anlas；图生图同样适用。同批其余图片付 Anlas，所以一次请求可以同时消耗两种额度。

官方额度仅在实际生成时按需确认；查询失败则暂停，确认耗尽后才重算全付费价格并检查权限及预算。
付费 V5 使用 1.5 倍基础价，在图生图强度折算后按单张向上取整，最低 2 Anlas。
V4 系列局部重绘按 `img2img.strength` 折算，缺省为 1；V5 Full 局部重绘按 2026-09-24 API 实扣样本使用完整基础价。

当前 V5 限制：

| 项目 | 当前值 |
| --- | ---: |
| 每把 Key 每日 V5 额度 | 50 张 |
| 全站每日 V5 额度 | 150 张 |

不符合上述 V5 额度条件时，改按 Anlas 估算扣费，不扣 V5 日额度。

## Anlas 限制

是否允许 Anlas 由管理员为每把 Key 单独决定。已开通 Anlas 的 Key 当前受以下限制：

| 项目 | 当前值 |
| --- | ---: |
| 每日 Anlas | 100 |
| 每月 Anlas | 2500 |
| 全站每月 Anlas 预算 | 10,000 |

## 全站保护

- 上游总并发：2；同一把上游 Token 的图片/标签请求仍至少间隔 15 秒。
- 图片生成与标签建议：每把上游 Token 各自至少间隔 15 秒。
- 任意图片上游出现 `429`：全站图片与标签请求立即暂停至少 60 秒；上游给出的 `Retry-After` 更长时，以更长者为准。
- 冷却状态会跨容器重启保留。

## 上游令牌池的独立保护

当站长在 `NAI_TOKENS` 中配置第二把上游令牌后，它受独立费用策略约束：

- 这把上游令牌自身对 V4.5 及更低模型的免费小图不设张数上限（每把用户 Key 仍默认限 100 张/日）；
- 免费 V5 小图初始每日最多 150 张（站长可在面板调整）；
- 任何会消耗 Anlas 的图片请求都不会分配给这把令牌。

上述上游额度不受虚拟 Key 的管理员权限绕过；这样即使站长自己的 Key 也不会意外消耗第二把令牌的 Anlas。请求仍同时受各虚拟 Key 的权限、额度及全站保护规则约束。

站长可在管理面板「总览 → 上游令牌池」单独修改任意上游 Token 的免费 V5 日限额（0 为不限）；设置按 Token 保存，不依赖其在 `NAI_TOKENS` 中的位置。`.env` 中的限额只作为尚未在面板保存过的 Token 的初始值。

站长也可在同一位置停用或启用某把上游 Token。停用后不再分配新请求，已发出的请求继续完成；设置会持久保存。启用操作不会清除该 Token 因上游 `401` 进入的失效状态。

## 异常消耗统计

生图（含流式）、放大、导演工具和 Vibe 编码已被上游接受但未收到完整有效结果，或发送后因读写中断、超时、服务端错误而结果不明时，
后台记录「待核实 Anlas」：请求预计费用减去已完成图片的记费。总览按配置时区统计今日、本月金额和笔数，用量日志可查每笔原因。
这是可能的上游扣款，不是官方实扣确认；不扣用户积分，也不混入已记费用和预算。统计不增加官方余额查询，历史失败日志不补猜金额。
派发前拒绝、连接失败和上游明确返回的 4xx 不计入；预计免费且失败的请求仍保留错误日志，待核实金额为 0。

## 图片隐私

- 成功生成的图片仅透传给请求方，本站不再保存图片文件或提供图片查看、下载功能。
- 不保存用户提示词。
